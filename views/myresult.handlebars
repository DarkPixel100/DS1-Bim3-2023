<!doctype html>
<html lang="pt-BR">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
        integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

    <title>Empregados</title>

</head>

<body>
    <div style="display: flex; flex-direction: column;">
        <form action="" method="post">
            <label for="search">
                Pesquisar nome: 
                <input type="search" id="search" name="nome" class="text-info">
            </label>
            <button class="btn" type="submit">🔍</button>
        </form>
        <ul>
            <h3>Pessoas por setor:</h3>
            <li>Administrativo: {{ count.admin }}</li>
            <li>Designer: {{ count.design }}</li>
            <li>Contábil: {{ count.conta }}</li>
            <li>Fábrica: {{ count.fab }}</li>
        </ul>
    </div>
    <table class="table">
        <thead>
            <tr>
                <form action="/show/" method="post">
                    <th scope="col"><button class="btn" type="submit" name="ordem" value="id">ID</button></th>
                    <th scope="col"><button class="btn" type="submit" name="ordem" value="nome">Nome</button></th>
                    <th scope="col"><button class="btn" type="submit" name="ordem" value="sal_bruto">Salário Bruto</button></th>
                    <th scope="col"><button class="btn" type="submit" name="ordem" value="sal_bruto">Salário Líquido</button></th>
                    <th scope="col"><button class="btn" type="submit" name="ordem" value="departamento"> Departamento </button></th>
                </form>
            </tr>
        </thead>

        <tbody>
            {{#each results }}
            <tr {{#ifEquals id minmax.max.id }} style="border: green 1px solid;" {{/ifEquals}}>
                <td>{{ id }}</td>
                <td>{{ nome }}</td>
                <td>{{parseReal sal_bruto }}</td>
                <td>{{parseReal sal_liquido }} </td>
                <td>{{ departamento }} </td>
                <td><a href="/edit/{{ id }}"><button class="btn btn-warning"> Edit </button></a></td>
                <td><a href="/delete/{{ id }}"><button class="btn btn-danger"> Delete </button></a></td>
            </tr>
            {{/each}}



        </tbody>

    </table>
</body>